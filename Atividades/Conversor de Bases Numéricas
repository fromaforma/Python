nume=int(input('escreva o número desejado: '))
escolha=int(input('''
    1 para binario
    2 para octal
    3 para hexadecimal
escreva qual base de conversão você deseja fazer com este número:'''))
if escolha>=4 and escolha<=0:
    print('este número não é valido digite outro número válido')

elif escolha==1:
    print('A conversão para binario ficará igual á:{}'.format(bin(nume)[2:]))

elif escolha==2:
    print('a conversão para octal ficará igual á:{}'.format(oct(nume)[2:]))

else:
    print('a conversão hexadecimal ficará igual á:{}'.format(hex(nume)[2:]))
